# Alternative requirements.txt - Minimal versions
# Use this if the optimized version still fails

streamlit==1.25.0
pandas==1.5.3
requests==2.28.2
beautifulsoup4==4.11.2

# Force specific numpy version to avoid conflicts
numpy==1.24.3